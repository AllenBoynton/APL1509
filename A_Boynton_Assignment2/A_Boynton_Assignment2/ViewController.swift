////  ViewController.swift//  A_Boynton_Assignment2////  Created by Allen Boynton on 9/10/15.//  Copyright (c) 2015 Full Sail University. All rights reserved.//import UIKit// Global constantslet firstNum: Int = 4let secondNum: Int = 6let thirdNum: Int = 4let string1 = "My job always took up all my time."let string2 = "So I quit to focus on school and my family."class ViewController: UIViewController{    override func viewDidLoad(){                // Created add function to add constants        func add(firstNum: Int, secondNum: Int) -> Int        {            return firstNum + secondNum        }                // Call the add function within the viewcontroller class to print line with the sum        var sum = add(firstNum, secondNum: secondNum)            // Print the sum of the integers        print ("The sum of the numbers is \(sum)")// I called on my initializers from each corresponding class to call and print results        // Initializer is automattically called to print result to see if the values are not equal        var notEquals = BoolFunction(firstNum: firstNum, secondNum: secondNum)        // Print false statement based on function        print ("The numbers \(firstNum) and \(secondNum) being equal is \(notEquals.compare(firstNum, secondNum: secondNum)).")                // Custom initializer is called to print result to see if the values are equal        var equals = BoolFunction(firstNum: firstNum, secondNum: secondNum)        // Print true statement based on function        print ("The numbers \(firstNum) and \(thirdNum) being equal is \(equals.compare(firstNum, secondNum: thirdNum)).")                // Initializer is automattically called to print the result to pass in both string values        var fullString = StringFunction(string1: string1, string2: string2)        // Print out returned value of concatenated strings with space        print ("\(fullString.concat(string1, string2: string2))")                super.viewDidLoad()    }    override func didReceiveMemoryWarning() {        super.didReceiveMemoryWarning()        // Dispose of any resources that can be recreated.    }    }